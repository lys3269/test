version: '3.0'

services:
  nginx-app:
    build: .
      
    image: ${DOCKER_USER_ID}/hw6

  production:

    image: ${DOCKER_USER_ID}/hw6:${BUILD_NUMBER}
    restart: always
    volumes:
      -"/home/jenkins:/var/jenkins_home "
    ports:
      - "80:80"